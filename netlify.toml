[build]
  command = "mkdir -p .netlify-publish/officehours .netlify-publish/savetheworld && cp index.html .netlify-publish/ && cp -R assets bidpoints css js apgovelections rides pujarinet kwatnoskiapgov .netlify-publish/ && cd savetheworld && npm ci && npm run build && cp -R dist/. ../.netlify-publish/savetheworld/ && cd ../officehours/ux-studio-screen-main && npm ci && npm run build && cp -R out/. ../../.netlify-publish/officehours/"
  publish = ".netlify-publish"
