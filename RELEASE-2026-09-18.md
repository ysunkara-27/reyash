# Task Pup + rides release — September 18, 2026, 08:00 America/New_York

User requested local commits now and remote pushes tomorrow morning. Do not push or deploy before 2026-09-18T12:00:00Z.

## Prepared repositories

- Portfolio + shared API: `.release/reyash` (a writable local release clone), `main`, remote `https://github.com/ysunkara-27/reyash.git`.
- Task Pup frontend: `.release/good-day`, `main`, remote `https://github.com/ysunkara-27/good-day.git`, prepared commit `07ad3e70dcf56b855625596d138878a07a0529c2`.
- The Task Pup release checkout preserves the standalone site's root URLs and its newer loading/recovery behavior. Do not overwrite it with the portfolio's entire `dog/` directory.
- Keep both `.release/good-day` and `.release/reyash` until their commits have been pushed. The source workspace remains edited because its Git metadata is read-only in this session. It is deliberately ignored by the portfolio repository.

## Release order

1. Verify the scheduled time has arrived, the prepared commits exist, and both destinations still point at the expected repositories. Fetch remote `main` and check fast-forward safety. Preserve any unrelated work. Never force-push.
2. Deploy the shared Cloudflare Worker from the prepared portfolio commit using `rides/api/wrangler.jsonc`. Use a clean checkout of that commit if the user's working tree has changed. This is the existing `hooraas-rides-api` Worker and existing D1 database; do not replace either. The release needs no secret changes or explicit schema migration: request-log and traffic-score tables are created idempotently.
3. Push the prepared portfolio release commit and Task Pup release commit to their respective `main` branches. Normal GitHub integrations build the frontends. Do not push later unrelated local commits. If remote work has advanced, reconcile only these feature changes without discarding remote work; report any unresolved blocker.
4. Check the remote branch SHAs and deployment status. Report actual results and pause the one-time automation after completion. Do not keep pushing on subsequent days.

## Checks completed

- 54 model/API tests passed, followed by passing tests for the newly added traffic leaderboard isolation and cat profile round-trip (56 distinct tests total).
- Standalone Task Pup: 30 model/API tests passed before the additional cat profile test; that shared profile test also passed.
- Both static builds passed. Frontend entry points bundled successfully. Shared Worker deploy dry-run passed.
- Desktop/mobile fixture browser suite: `node scripts/test-playful-sites.mjs`, with a static server on port 8106. It could not run in this session: headless Chrome terminated under the sandbox and automatic approval review denied opening the local browser preview. No visual/browser pass is claimed. Do not retry blocked browser access without new authorization.
- Existing earned-care browser test was updated for the new meal-picker confirmation.

## User-facing changes

Task Pup has a dedicated companion club, meal picker, ball play and pats, earned tricks, XP/levels and streaks, cats/dogs unlocked by best streak, permanent companion collection, hourly encouragement, and an authenticated `yash` preview playground. Preview actions do not manufacture saved task/care progress. Rides has a larger mobile-friendly three-lane traffic game with cars, cones, potholes, stars, explicit lane controls, pause, and its own leaderboard. Both have a compact public Ideas & requests dialog backed by separate logs.
